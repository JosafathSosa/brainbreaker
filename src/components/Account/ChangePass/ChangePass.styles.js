import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  btnContainer: {
    marginHorizontal: 20,
    borderRadius: 10,
    marginTop: 20,
  },

  btn: {
    backgroundColor: "#332419",
    height: 50,
  },
});
