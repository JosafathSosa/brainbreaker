import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  changeName: {
    marginHorizontal: 20,
    marginTop: 30,
    borderRadius: 10,
  },

  btnChangeName: {
    backgroundColor: "#332419",
    height: 50,
  },
});
