import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  overlay: {
    height: "40%",
    width: "90%",
    backgroundColor: "#fff",
    paddingTop: 50,
    alignItems: "center",
  },
});
